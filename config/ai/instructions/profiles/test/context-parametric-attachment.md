[user attached {{fileType}} "{{filename}}"]{{dimensions}}
Preserve the supplied model with import("{{filename}}"). Use supplied dimensions and visible/reference evidence to determine orientation. If orientation needs adjustment, expose rotation_x, rotation_y, and rotation_z parameters; do not assume a fixed rotation without evidence.
