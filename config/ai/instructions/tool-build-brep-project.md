Create or update the complete canonical native BRep project snapshot.

The `project` field is the full `BrepProject`, never a patch. For follow-up edits preserve the existing project ID and preserve every unchanged node, published-parameter ID, project-object role assignment and semantic point ID. Use new IDs only for genuinely new nodes, parameters or semantic points, and remove IDs only when those semantic objects are intentionally removed.

Use only the feature, parameter, placement, metadata, project-object, scalar-expression and selector forms exposed by the tool schema. Never return build123d/Python source, OCCT/runtime objects, STEP data, viewer mesh/tessellation, filesystem paths, raw expression/source strings, or raw topology identifiers such as edge/face indices.

`resultNodeId` remains the canonical primary BRep result. `projectObject.footprintNodeId`, `clearanceEnvelopeNodeId` and `maintenanceEnvelopeNodeId` are optional semantic auxiliary roles that reference existing feature-node IDs; assigning a role does not replace or implicitly change `resultNodeId`. Project-object semantic points are stable local component data: `position` uses millimetre-compatible scalars, optional `direction` uses unitless-compatible scalars, and `kind` is `connection`, `mounting` or `cable`. Preserve existing semantic point IDs when editing their label, kind, position or direction.

Every feature node must contribute, through its dependency chain, to `resultNodeId` or to an explicit project-object geometry role. Do not leave disconnected/orphan feature branches such as a finishing feature whose predecessor remains the authoritative Result. Every published parameter must either affect a feature in one of those authoritative geometry dependency chains or intentionally control project placement or semantic point data. Do not publish duplicate, placeholder, disconnected or otherwise ineffective geometry controls; remove such parameters instead of exposing sliders that cannot affect authoritative geometry.

Scalar fields may be a finite numeric literal, a direct published-parameter reference such as `{ "parameter": "width" }`, or a bounded expression AST. Binary expressions use exactly two arguments: `{ "op": "add" | "sub" | "mul" | "div", "args": [left, right] }`. Unary negation uses exactly one argument: `{ "op": "neg", "args": [value] }`. Never invent expression strings, `eval`, source code, arbitrary variables, arbitrary functions or other operators. Keep expression trees small and bounded; the canonical validator enforces maximum depth/node count, finite intermediate values and division-by-zero safety.

Respect scalar units. `add` and `sub` require compatible equal dimensions. `mul` is only for dimensionless scaling (`none * X` or `X * none`). `div` may divide by a dimensionless scalar (`X / none -> X`) or divide equal dimensions (`X / X -> none`). Do not create implicit area/mixed-dimension expressions such as `mm * mm` or `deg * mm`. Numeric literals are contextually typed by the target expression. Do not construct a divisor whose default or normal parameter range can become zero.

Use scalar expressions for genuine derived relationships instead of publishing fake dependent sliders. For example, represent `innerWidth = overallWidth - 2 * wallThickness` as `{ "op": "sub", "args": [{ "parameter": "overallWidth" }, { "op": "mul", "args": [{ "parameter": "wallThickness" }, 2] }] }`. Only independent user-facing inputs should normally become published parameters.

Native primitive coordinate semantics are centered-origin semantics. A `box` is centered on its local origin, so width W and depth D occupy X = -W/2..W/2 and Y = -D/2..D/2. A `cylinder` is likewise centered on its local origin. `transform.translate` is a displacement from that centered local origin; it is not an absolute coordinate measured from a box minimum corner. Therefore a concentric/centered cylindrical through-hole in a centered box normally uses the same local origin (for example translate `[0, 0, 0]`), not `[W/2, D/2, 0]`. Use the bounded scalar AST for arithmetic such as half-widths or clearance offsets instead of freezing those relationships into numeric literals.

`transform.rotateDeg = [rx, ry, rz]` uses the canonical M6 rotation convention. Angles are degree-compatible bounded M1 scalars and may be literals, published-parameter references or bounded scalar expressions. The tuple is interpreted as build123d/OCCT-style **Intrinsic XYZ** rotation about the canonical local origin. Positive angles follow the ordinary right-hand rule. Conceptually, the local geometry is first rotated by the Intrinsic-XYZ orientation and then translated by `transform.translate`, so a local point follows `p' = R_intrinsicXYZ * p + T`. Do not reinterpret the tuple as arbitrary extrinsic XYZ, yaw/pitch/roll strings, axis-angle, quaternion or matrix data. A transform still requires a single-shape input and returns one `single`; it does not accept an `instanceSet`. When translation is omitted use zero translation; when rotation is omitted use zero rotation. Keep derived angle relationships in the existing bounded degree-scalar AST rather than baking them into literals.

`extrude` is the bounded M4 profile-to-solid feature. Its `profile` is inline canonical data, not a separate graph node or reusable non-solid result. Supported profile variants are `{ "type": "rectangle", "width": ..., "height": ... }`, `{ "type": "circle", "radius": ... }`, and `{ "type": "closedPolyline", "points": [{ "u": ..., "v": ... }, ...] }`. Rectangle width/height, circle radius, every closed-polyline U/V coordinate and extrusion `depth` may be millimetre-compatible literals, published-parameter references or bounded scalar expressions. Rectangle dimensions, circle radius and extrusion depth must resolve strictly positive. A closed polyline must contain 3 through 32 ordered vertices; closure from the final vertex back to the first is implicit, so do not repeat the first vertex at the end. At effective parameter values it must remain a simple non-degenerate loop: no zero-length edges, zero area or self-intersection. The profile is centered on the local origin for rectangle/circle; closed-polyline coordinates are explicit in the local profile plane. `axis: "x"` means U=Y, V=Z and normal +X; `axis: "y"` means U=Z, V=X and normal +Y; `axis: "z"` means U=X, V=Y and normal +Z. Extrusion is symmetric from `-depth/2` through `+depth/2` along that normal and always returns one `single` solid. It can therefore be consumed wherever another single-shape node is allowed. Do not invent arbitrary workplanes, arbitrary extrusion vectors, open profiles, holes/multiple loops, profile/sketch graph nodes, constraints, splines/NURBS or topology references; those are outside the current M4 surface.

`union` and `intersect` are bounded ordered Boolean composition nodes. Their `inputs` array must contain between 2 and 32 unique existing node IDs. Keep the intended evaluation order explicit even when the mathematical operation is commutative. A Boolean composition is valid only when native OCCT and the Rhino/Grasshopper host path can resolve the final operation to exactly one solid/Brep. Do not use `union` to represent intentionally disjoint assemblies or multi-body output, and do not use `intersect` when the intended result is empty. If the requested model requires multiple independent bodies, an empty Boolean, or unsupported result cardinality, do not disguise it as a successful single-body project.

`linearPattern` is the bounded one-dimensional multi-instance composition node. It references exactly one existing single-shape `input`, uses `axis: "x" | "y" | "z"`, a literal integer `count` from 2 through 32, and a non-zero millimetre-compatible `spacing` scalar. `spacing` may be a literal, published-parameter reference or bounded M1 scalar expression. Instance index 0 is the unshifted input; later instances are translated by `index * spacing` along the selected axis, preserving canonical index order. A linear pattern returns an `instanceSet`, not one fused solid. It may be the primary `resultNodeId`, in which case Brepia and Grasshopper expose all instances as separate ordered bodies/Breps. It may also be used as a `subtract.tools[]` entry; subtraction then applies every pattern instance as an ordered cutter. Do not use an instance-set node as the input/base of `transform`, `mirror`, `fillet`, another pattern, `union`, `intersect`, or `subtract.base`, and do not assign it to project-object geometry roles, because those surfaces require one shape. Do not replace a requested repeated multi-body result with a Boolean union merely to satisfy single-body semantics.

`rectangularPattern` is the bounded two-dimensional multi-instance composition node and reuses the same `instanceSet` result model as `linearPattern`. It references exactly one existing single-shape `input`. `axisA` and `axisB` must each be `"x" | "y" | "z"` and must be different. `countA` and `countB` are literal integers from 2 through 32 and their product must not exceed 64. `spacingA` and `spacingB` are independent non-zero millimetre-compatible scalars and may each be literals, published-parameter references or bounded M1 scalar expressions. Instance `(a,b)` is translated by `a * spacingA` along `axisA` plus `b * spacingB` along `axisB`. Ordering is row-major with `a` as the outer loop and `b` as the inner loop, so the stable flat index is `a * countB + b`; Brepia body identity is `<patternId>::<index>`. A rectangular pattern may be the primary `resultNodeId`, exposing the ordered instances separately, or one `subtract.tools[]` entry, applying every ordered instance as a cutter. It may not consume a `linearPattern`, another `rectangularPattern`, or any other `instanceSet`, and no single-only consumer may consume its result. Do not invent nested/general collection algebra or implicitly union pattern instances.

`mirror` is a single-shape symmetry node. It references exactly one existing `input`, returns only the mirrored shape, and never implicitly keeps or unions the original. `normalAxis: "x"` means a YZ mirror plane, `"y"` means XZ and `"z"` means XY. `offset` is the mirror-plane position in millimetres along that normal axis and may be a literal, parameter reference or bounded M1 scalar expression. Use `mirror` when the requested result is the reflected counterpart of a shape. If the user wants both the original and mirrored copy, model those as two explicit branches and combine them only with a supported operation whose semantics are actually correct; do not treat `mirror` itself as a two-instance pattern or collection.

Before returning a project, perform a compact spatial sanity pass at the published default parameter values. Resolve the important extrusion, transform, mirror and pattern expressions conceptually and check centered half-extents. For a transform, resolve `[rx, ry, rz]` using the Intrinsic-XYZ convention, rotate about the local origin first, then apply translation; do not mentally translate first and rotate the translation vector. For an `extrude`, verify positive effective dimensions/depth, the requested X/Y/Z profile frame, simple closed-polyline geometry where applicable, and the symmetric `-depth/2..+depth/2` span. Every `subtract` tool that is intended to remove material must overlap the target material at defaults; if the tool is a pattern, at least the intended cutter instances must intersect the base rather than relying on silent no-op geometry. `union` inputs intended to create one body must overlap or otherwise form one valid connected result at defaults, and `intersect` inputs must have a non-empty common overlap at defaults. For a mirror node, verify that its axis and offset represent the intended symmetry plane and remember that the original shape is not retained automatically. For a linear pattern, verify that `count` and resolved non-zero `spacing` produce the intended index-ordered copies without confusing total span with center-to-center spacing. For a rectangular pattern, verify distinct axes, the `countA * countB <= 64` bound, both resolved non-zero spacings, and the intended row-major two-axis placement. For a centered room, enclosure or similar shell, derive wall locations from half-extents rather than from a full dimension: for example the center of the negative-X wall slab is `wallThickness / 2 - roomWidth / 2`, not `-roomWidth`. Door/window cutters should cross the complete wall thickness with a small bounded clearance, and an ordinary doorway should reach the floor unless the user explicitly requests a sill or threshold. For an object intended to sit flush against an interior wall face, derive its center from that interior face plus or minus half the object's depth; do not confuse an object's full depth with its center offset. Prefer simple, geometrically meaningful CSG over fragile coordinate tricks.

The returned snapshot must be internally valid: all node/parameter/project-object references must resolve, the DAG must be acyclic, value kinds must be compatible, `resultNodeId` must exist, parameter units/ranges must be valid, scalar expressions must satisfy the bounded AST/unit rules, every transform rotation must use the canonical Intrinsic-XYZ degree tuple semantics, every extrusion profile/depth must remain valid at effective values, every linear-pattern spacing and both rectangular-pattern spacings must resolve to non-zero millimetre values, rectangular-pattern axes/count/product rules must hold, topology selectors must use only canonical semantic selector forms, graph reachability must contain no orphan feature nodes, and published parameters must satisfy the effectiveness rules above. If the requested edit cannot be represented by the current BRep schema, do not invent fields or runtime code.