# Bounded full revolve — installed Rhino 8 / Grasshopper runtime evidence

Status: **Gate C accepted — installed Rhino 8 / Grasshopper runtime PASS**

Date: 2026-09-12

Repository: `weaf/brepia`

Branch: `feature/brep-grasshopper-gh-packaging`

PR #36 remains intentionally open, draft, stacked on `feature/brep-grasshopper-smart-component` and unmerged.

## Evidence boundary

This document records only the installed-host acceptance layer for the bounded first full-revolve slice.

Repository/CI evidence and pinned native build123d/OCCT evidence remain separate:

```text
Gate A: repository / CI
Gate B: pinned native build123d / OCCT
Gate C: installed Rhino 8 / Grasshopper
```

Gate A and Gate B were already accepted before this run. This document does not use repository tests or native execution as substitutes for Rhino host evidence.

## Source checkpoint and repository gate

The fresh installed-host fixtures were generated from:

```text
679bdc015707e6bb130e19979ec92af523279037
Add bounded revolve Rhino Gate C acceptance tooling
```

Repository CI on that exact checkpoint passed separately:

```text
Quality Gate #1085       PASS
Grasshopper Build #657   PASS
```

Those CI results establish only the repository/tooling baseline. The host evidence below came from the installed Rhino 8 / Grasshopper environment.

## Installed host

Target host:

```text
Rhino 8
Grasshopper bundled with the installed Rhino 8 host
```

The exact Rhino 8 point release was not separately captured in the returned validator output, so this evidence deliberately records the supported product target as Rhino 8 rather than inventing a more specific version.

## Fresh current-compiler fixtures

The Gate C utility generated:

```text
revolve-parameterized-z.ghx
revolve-axis-adjacent-y.ghx
```

using:

```bash
./scripts/brep/revolve-rhino-acceptance.sh generate
```

Both definitions are generated by the current executable GHX compiler and validate as Result **Item** definitions before host execution.

### Parameterized Z revolve

The primary fixture uses the locked first-slice revolve contract:

- canonical Z axis;
- full 360 degree revolution;
- `closedPolyline` profile;
- `u` axial and `v` radial;
- through-bore inner radius 8;
- published radial and axial controls;
- ordinary `single` result / Grasshopper Result Item access.

Generated defaults:

```text
outerRadius = 18 mm
length      = 40 mm
```

Installed-host perturbation:

```text
outerRadius: 18 -> 22 mm
length:      40 -> 52 mm
```

The installed Rhino 8 / Grasshopper acceptance run was reported green. The model opened and solved in the target host, the full revolution produced the intended axisymmetric turned geometry, the radial and axial parameter changes recomputed the geometry, and the host-saved definition was closed and reopened before return validation.

### Axis-adjacent Y revolve

The second fixture uses canonical Y and the locked supported axis-contact case:

```text
(-20,  0)
(-20, 12)
( -5, 12)
( -5,  9)
( 20,  9)
( 20,  0)
```

Its closing boundary contains a non-zero segment on `v=0`. The installed-host run was reported green and the resulting definition remained an ordinary Result **Item**.

This separately exercises the supported axis-adjacent profile case already accepted in the pinned native runtime.

## Save -> close -> reopen and strict returned-GHX validation

The host-saved files were returned as:

```text
revolve-parameterized-z-returned.ghx
revolve-axis-adjacent-y-returned.ghx
```

They were validated with:

```bash
./scripts/brep/revolve-rhino-acceptance.sh validate \
  tmp/revolve-rhino-acceptance/revolve-parameterized-z-returned.ghx \
  tmp/revolve-rhino-acceptance/revolve-axis-adjacent-y-returned.ghx \
  22 52
```

Observed output:

```text
RUN  v4.1.11 /home/thn/ai/pCAD

stdout | tests/brepRevolveRhinoAcceptanceTool.test.ts > bounded revolve Rhino acceptance tooling > strictly validates installed-host returned revolve GHX files
{"returnedValidation":"accepted","parameterized":{"kind":"parameterized","filename":"revolve-parameterized-z-returned.ghx","parameters":{"length":52,"outerRadius":22},"expectedResultAccess":"Item"},"axisAdjacent":{"kind":"axisAdjacent","filename":"revolve-axis-adjacent-y-returned.ghx","parameters":{},"expectedResultAccess":"Item"}}

✓ tests/brepRevolveRhinoAcceptanceTool.test.ts > bounded revolve Rhino acceptance tooling > strictly validates installed-host returned revolve GHX files 18ms

Test Files  1 passed (1)
Tests       1 passed (1)
```

Acceptance consequences:

- the parameterized returned GHX persisted `outerRadius = 22`;
- the parameterized returned GHX persisted `length = 52`;
- the axis-adjacent returned GHX contained no unexpected published parameter state;
- both returned files retained expected Result **Item** access;
- both returned files passed the strict returned-document validator;
- generated Python source, component identity, port identity, graph shape, wiring, type hints, runtime settings and Brepia-owned output semantics remained inside the existing strict compatibility boundary;
- no arbitrary Grasshopper graph/script mutation was accepted;
- the GHX return boundary remains parameter-only.

## Rhino compiler path exercised

The accepted definitions exercise the current built-in Rhino 8 Python 3 carrier and RhinoCommon revolve path:

```text
explicit locked U/V profile plane
-> closed profile curve
-> canonical X/Y/Z axis through local origin
-> rg.RevSurface.Create(profile, axis)
-> rg.Brep.CreateFromRevSurface(...)
-> require solid Brep
-> Brepia placement
-> Result Item
```

The host run therefore provides runtime evidence for the actual current compiler path rather than generated-source inspection alone.

## Preserved bounded contract

Gate C does not broaden the canonical surface. The accepted contract remains:

- full 360 degree revolve only;
- no partial or start angle;
- X/Y/Z canonical axes only;
- no arbitrary vector or topology-attached axis;
- existing inline M4 `BrepProfile` grammar;
- first-slice revolve uses `closedPolyline`;
- centered M4 rectangle/circle continue to work for extrusion but remain rejected for first-slice revolve;
- resolved radial `v >= 0`;
- axis contact only through a non-zero boundary segment on `v=0`;
- isolated point-only axis contact remains unsupported;
- exactly one positive-volume solid/Brep;
- `schemaVersion: 1` unchanged;
- no new M1 functions;
- `single | instanceSet` discipline unchanged;
- build123d/OCCT remains geometry authority;
- Rhino/GHX remains interoperability compiler/runtime evidence;
- GHX return remains parameter-only.

## Gate C conclusion

Gate C is accepted.

Bounded full revolve is now accepted across all three required evidence layers:

1. repository / CI;
2. real pinned rootless build123d / OCCT runtime;
3. installed Rhino 8 / Grasshopper runtime, including parameter perturbation, host persistence and strict returned-GHX validation.

The bounded full-revolve slice is therefore fully accepted without changing its locked canonical boundary.
